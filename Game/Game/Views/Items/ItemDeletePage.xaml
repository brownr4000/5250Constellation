<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="Game.Views.ItemDeletePage"
             Title="{Binding Title}">

    <StackLayout Padding="20" Spacing="10" HorizontalOptions="Center">

        <Label Text="Are you sure you want to delete this item? This action cannot be undone."/>
        <StackLayout Orientation="Vertical" HorizontalOptions="Center">
            <Image HeightRequest="150" Source="{Binding Data.ImageURI}" Style="{StaticResource ImageLargeStyle}"/>
            <Label HorizontalTextAlignment="Center" Text="{Binding Data.Name}"/>
        </StackLayout>

        <StackLayout HorizontalOptions="Center" Padding="20">
            <Button BackgroundColor="Gray" HeightRequest="50" WidthRequest="150" Text="Delete Item" Clicked="Delete_Clicked"/>
            <Button BackgroundColor="Gray" HeightRequest="50" WidthRequest="150" Text="Back to Menu" Clicked="Cancel_Clicked"/>
        </StackLayout>

    </StackLayout>

</ContentPage>